int main(){
    List(int, 100) fib;
    int i;

    fib[0] = 0;
    fib[1] = 1;

    for(i=2; i<100; i++){
        fib[i] = fib[i - 1] + fib[i - 2];
    }
    
    for(i=0; i<10; i++){
        print(fib[i]);
    }

    return 0;
    
}